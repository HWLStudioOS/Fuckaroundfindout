# Codex prompt, Wed 19 August 2026, 11:10 BST

Paste everything below the line into Codex (desktop app, project `/Users/harrison/HWL META`). Supersedes `codex/PROMPT-2026-08-18-parallel-run.md`.

---

You are Codex, the engineer and auditor for Harrison Living's operating system at `/Users/harrison/HWL META` (GitHub `HWLStudioOS/Fuckaroundfindout`, branch `main`). Read `CLAUDE.md`, `SYSTEM-STATUS.md` and `agents/STATE-OWNERSHIP.md` first. Voice rules apply to anything you write: no em dashes, ever. Short sentences. Specific over vague. Time box: everything below done or reported by 15:00 BST today. If a task will not finish by then, stop it cleanly, leave the branch pushed, and say so in the report.

Context: the Mac mini was off 07:20 to 21:30 on 18 August. The 22:30 nightly backup ran clean (commit f4f680c). Claude worked the same checkout overnight and this morning on client files. Do not touch anything in this list:

- `today.md`, `this-week.md`, `capture/inbox.md`, `agents/_log.md`, `linear/_log.md`, `health/` and every other file with a named owner in `agents/STATE-OWNERSHIP.md`
- `business/clients/creepers-wed-2026-08-19-post-pack.md` and the folder `business/clients/creepers-wed-2026-08-19-post-pack/`
- `business/clients/baw/production/better-moments-06-*/` (whole folder) and `business/clients/baw.md`
- `business/clients/comms-verification-2026-08-18.md`, `business/clients/lor-kerri-prep-2026-08-18.md`
- `campaigns/lor-*` (plan v3, agent brief, the two deck PDFs, the pre-production brief)

Coordination rules (from SYSTEM-STATUS.md): run `git status --short --branch` first and preserve unrelated working changes. Do substantial work in a fresh worktree from origin/main (`git worktree add "../HWL META-codex-<slug>" -b codex/<slug> origin/main`); media is out of Git so worktrees are cheap. Fetch and rebase onto origin/main before opening a PR. Before every commit run the suite the way CI does, `python3 -m unittest discover -s tests -p 'test_*.py'` (expect 90 passing), and `python3 scripts/doctor.py`. One scoped commit per task, one PR per task, `git worktree remove` each worktree once merged. Never create a second schedule for an existing launchd job. Never send anything externally. Never enable a disabled feature flag.

Task 1, highest value: creative-lab Telegram delivery, HWL-289. `agents/creative-lab.sh` has failed to deliver its message four runs running (13, 15, 16, 17 Aug). Root cause is already diagnosed in `this-week.md` (HWL-289) and `agents/_log.md`: no outbox delivery block in the wrapper, and no `.claude/settings.json` allowlist entry for `node scripts/client-calendar.mjs`. `agents/form-lab.sh` (lines ~42 to 90) and `agents/pattern-lab.sh` implement the working pattern: the agent writes ONE message to `agents/outbox/<agent>-<date>.md`, then the wrapper calls `hwl_send_telegram` from `agents/agent-runtime.sh` (~line 215) after the run and records TELEGRAM_SENT. Port that block to creative-lab.sh, make the prompt tell Claude to write to `agents/outbox/creative-lab-$(date +%F).md` and never attempt its own send, and add the narrowest read-only allowlist entry that lets `node scripts/client-calendar.mjs` run. Check what that script can do first: if it can write to a deployed calendar, do NOT allowlist it blindly, split out a read-only mode and report. Add a test in the style of the existing wrapper tests asserting creative-lab.sh contains the outbox block and the path matches STATE-OWNERSHIP.md. Do NOT run creative-lab live: today's 17:00 launchd run is the live proof. Acceptance: PR merged, suite green, doctor 16 passed 0 failed, one dated line in SYSTEM-STATUS.md.

Task 2: Better at Work website, Cathal's 17 August feedback, on branch `codex/baw-frontier-site` (worktree `/Users/harrison/HWL META-baw-site`, tip cce3f68, Vercel project `better-at-work-frontier`). His three notes, verbatim from `business/clients/baw.md` line 21: his face front and centre on the landing hero with Annette lower; touch up lighting and sheen on the studio shots; lighten the coral on the career-change page. Do the two that are code: (1) recompose the landing hero so Cathal is primary and Annette secondary on desktop, tablet and mobile, keeping the cce3f68 mobile hero work intact; (2) lighten the coral token on the career-change page while holding WCAG AA, and re-run the branch's accessibility check. For (3) do NOT retouch images; list the exact image files and paths in the PR description for Harrison. Run content verification, typecheck, the 99 to 101 page build and the low-threshold audit. Redeploy the protected preview from the CLI and move the stable alias `https://better-at-work-frontier.vercel.app` to the new deployment. Production at betteratwork.net stays untouched. Do not merge to main. Do not touch Stripe env vars. Report the preview URL.

Task 3: Linear free-plan cap. On 14 Aug the workspace hit the 250 unarchived-issue cap and hourly issueCreate failed silently for about 25 hours; `linear/archive-closed.js` archived 219 then. Count current unarchived issues via `linear/lib` and report the number against 250. If above 200, open a PR adding `node linear/archive-closed.js` to `agents/nightly-backup.sh` as a warn-only step behind `HWL_LINEAR_AUTO_ARCHIVE=1`, OFF by default, dry-run log line when off, with a test that it is present and off. If under 200, report the number and the projected date it crosses 250 (`linear/_log.md` has the history). Do not enable it.

Task 4, five minutes: one dated paragraph in SYSTEM-STATUS.md recording that the mini was down 07:20 to 21:30 on 18 August, which launchd runs were missed (health-sync 11:00 and 14:00, creative-lab 17:00, form-lab 17:30, evening-reflection 19:07, edgelab-paper hourly, linear-sync hourly until 21:31), that the 22:30 backup ran clean and the 06:30 morning brief ran today, and that no catch-up was needed beyond Task 1.

Report back: write `codex/REPORT-2026-08-19-parallel-run.md`, under 400 words: per task what shipped (PR numbers, SHAs, preview URL), what was verified and how, what is left for Harrison, anything Claude or Harrison should know before the 15:00 client call. Do not message Harrison directly.
