# Codex parallel run, Tuesday 18 August 2026 evening

Paste everything below the line into Codex. Written by Claude (Fable 5) at 22:00 BST while Claude works the client and comms loops in the same repo. Codex takes the engineering loops so both agents finish by morning.

---

You are Codex, the engineer and auditor for Harrison Living's operating system at `/Users/harrison/HWL META` (GitHub `HWLStudioOS/Fuckaroundfindout`, branch `main`). Read `CLAUDE.md`, `SYSTEM-STATUS.md` and `agents/STATE-OWNERSHIP.md` first. Voice rules apply to anything you write: no em dashes, ever. Short sentences. Specific over vague.

Context for tonight: the Mac mini was powered off from about 07:20 to 21:30 BST on 18 August (Harrison was at a family hospital situation all day). Missed launchd runs today: `health-sync` 11:00 and 14:00, `creative-lab` 17:00, `form-lab` 17:30, `evening-reflection` 19:07, `edgelab-paper` hourly, `linear-sync` hourly (it caught up at 21:31 on load). `morning-brief` did run at 06:47. `nightly-backup` runs at 22:30 tonight. Claude (a separate agent, separate context) is concurrently working in the SAME checkout on client files. Do not touch anything Claude is writing tonight:

- `business/clients/creepers-wed-2026-08-19-post-pack.md`
- `business/clients/baw/production/better-moments-06-*/` (entire folder)
- `business/clients/comms-verification-2026-08-18.md`
- `today.md`, `this-week.md`, `capture/inbox.md`, `agents/_log.md` and every other file in `agents/STATE-OWNERSHIP.md` that has a named owner

Coordination rules (from `SYSTEM-STATUS.md`): run `git status --short --branch` first. Preserve unrelated working changes. Do all substantial work in a fresh worktree from `origin/main` (`git worktree add ../HWL\ META-codex-<slug> -b codex/<slug> origin/main`), which is cheap now that media is out of Git. Fetch and rebase onto `origin/main` again before you open the PR, because the 22:30 nightly backup will land a commit under you. Run the test suite (`python3 -m unittest discover -s tests -p 'test_*.py'`, the same command CI uses, expect 90 passing) and `python3 scripts/doctor.py` before every commit. One scoped commit per task, one PR per task, remove each worktree with `git worktree remove` once its PR is merged. Never create a second schedule for an existing launchd job. Never send anything externally.

## Task 1 (highest value): fix creative-lab Telegram delivery, HWL-289

`agents/creative-lab.sh` has failed to deliver its Telegram message four runs in a row (13, 15, 16, 17 Aug). Root cause is already diagnosed in `this-week.md` (HWL-289) and `agents/_log.md`: the wrapper has no outbox delivery block, and there is no `.claude/settings.json` allowlist entry for `node scripts/client-calendar.mjs`, so the agent falls back to denied curl/osascript/WebFetch sends. `agents/form-lab.sh` (lines ~42 to 90) and `agents/pattern-lab.sh` already implement the pattern: the agent writes ONE message to `agents/outbox/<agent>-<date>.md`, then the wrapper calls `hwl_send_telegram` from `agents/agent-runtime.sh` (line ~215) after the run and records `TELEGRAM_SENT`. Port that exact block to `creative-lab.sh`, make the prompt tell Claude to write the message to `agents/outbox/creative-lab-$(date +%F).md` and never attempt its own send, and add the narrowest read-only allowlist entry that lets `node scripts/client-calendar.mjs` run (check what the script actually does first; if it can write to the deployed calendar, do NOT allowlist it blindly, split a read-only mode instead and report). Add a test in the style of the existing wrapper tests (`tests/`) that asserts creative-lab.sh contains the outbox delivery block and that the outbox path convention matches `agents/STATE-OWNERSHIP.md`. Update the `agents/outbox/*.md` row note in `STATE-OWNERSHIP.md` only if the convention changes. Do NOT run creative-lab live tonight; the next scheduled run (Wed 17:00) is the live proof. Note in the PR that the 16 and 17 Aug undelivered messages sit at `agents/outbox/creative-lab-2026-08-16.md` and `-17.md`, and deliver the 17 Aug one through `hwl_send_telegram` as the live test of the helper only if the helper is exercisable non-interactively from your session (form-lab did exactly this on 14 Aug). If not, say so.

Acceptance: PR merged to `main`, suite green (90 or 91 tests), doctor 16 passed 0 failed, `SYSTEM-STATUS.md` gets one dated line under Live rollout state.

## Task 2: Linear free-plan cap, recurrence check and the archive step

On 14 August the workspace hit Linear's 250 unarchived-issue cap and hourly `issueCreate` failed silently for ~25 hours. `linear/archive-closed.js` archived 219 issues then. `SYSTEM-STATUS.md` says the cap returns in roughly three weeks and that the fix (a periodic archive step in the nightly backup, or a plan upgrade) is Harrison's call. Tonight: (a) count current unarchived issues via the existing Linear client in `linear/lib` and report the number against 250; (b) if the count is above 200, propose the archive step as a PR that adds `node linear/archive-closed.js` to `agents/nightly-backup.sh` as a warn-only step (never blocks the backup, same posture as `scripts/check-state-consistency.mjs`), guarded by an env flag `HWL_LINEAR_AUTO_ARCHIVE=1` that is OFF by default, with a dry-run log line when off. That way Harrison's call is one tap (set the flag) rather than a build. Do not enable it. Add a test that the step is present and off by default. If the count is under 200, skip the PR and just report the number and the projected date it crosses 250 at the current creation rate (`linear/_log.md` has the history).

## Task 3: Better at Work website, apply Cathal's 17 August feedback

The live BaW site branch is `codex/baw-frontier-site` (worktree `/Users/harrison/HWL META-baw-site`, tip `cce3f68`, Vercel project `better-at-work-frontier`, locked CLI preview deploys, see `business/clients/baw.md` "Season 5 close" and "Reprice call outcome, 17 August"). Cathal's feedback on the 17 Aug call, verbatim from `baw.md`: his face front and centre on the landing hero (PR advisor said the same, TV ambitions), Annette lower; touch up lighting and sheen on the studio shots; lighten the coral on the career-change page. Do the two that are code: (1) recompose the landing hero so Cathal is the primary portrait and Annette secondary, on desktop, tablet and mobile (the mobile hero was already recomposed once at `cce3f68`, keep that work intact); (2) lighten the coral token used on the career-change page, keeping WCAG AA contrast (re-run whatever accessibility check the branch already has, the 6 Aug pass fixed defects and must not regress). For (3) the studio-shot lighting/sheen, do NOT attempt image retouching; list the exact image files and their paths in the PR description so Harrison can retouch them in one pass. Run the branch's content verification, typecheck, build (expect the 99 to 101 page build) and low-threshold audit. Redeploy the protected preview from the CLI and move the stable alias `https://better-at-work-frontier.vercel.app` to the new deployment (baw.md notes the alias does not move on its own). Production at betteratwork.net stays untouched. Do not merge this branch to `main`. Do not touch Stripe env vars.

## Task 4: post-reboot health check and tonight's backup

After 22:35 BST, read the tail of `agents/_log.md` and `agents/logs/nightly-backup.stdout.log`. Confirm the 22:30 backup committed, pushed and verified the pushed SHA, and that the Board Room deploy passed. If it failed, diagnose from the log (do not re-run it blindly; if it is a transient push race with a Codex or Claude push, one manual `bash agents/nightly-backup.sh` re-run is acceptable, otherwise report). Also confirm `com.hwl.telegram-agent` and `com.hwl.baseline-api` are running (`launchctl list | grep hwl`) and `claude auth status --text` is logged in. Then note in `SYSTEM-STATUS.md` (one dated paragraph) that the mini was down 07:20 to 21:30 on 18 August, which scheduled runs were missed, and that no catch-up was needed except what you did.

## Report back

When done, write a short report to `codex/REPORT-2026-08-18-parallel-run.md`: per task, what shipped (PR numbers, commit SHAs), what was verified and how, what is left for Harrison (one tap or a decision), and anything you found that Claude or Harrison should know in the morning. Keep it under 400 words. Do not message Harrison directly; the morning brief and Claude will read the report.
