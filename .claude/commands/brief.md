# Brief

Short alias for the morning briefing, made for firing from Telegram on the phone.

Invoke the `morning-brief` skill now and return its output. If any argument was
passed after `/brief` (for example a date or "quick"), forward it to that skill as
its argument.
