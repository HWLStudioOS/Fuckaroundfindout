# System Status

Current engineering handoff for Harrison, Claude, and Codex. Read this on session start. Update it only when system architecture, rollout state, automation ownership, or safety boundaries change. Daily business state stays in `today.md` and the domain files.

Last updated: 14 August 2026

## Operating model

- Claude is the currently deployed operator. It handles Telegram and scheduled workflows running through launchd on the Mac mini.
- Codex is the engineer and auditor. It develops in a separate worktree or branch, runs tests and diagnostics, then merges reviewed commits.
- Git and Markdown hold authored knowledge and durable handoffs.
- Ignored SQLite databases hold mutable events, tasks, runs, queue leases, proposals, and approvals.
- Claude and Codex are separate workers. Sharing a repository does not give either agent the other agent's conversation or in-memory state.

## Automation ownership

Launchd remains the only production scheduler for the existing workflows. Do not create Codex, cron, or second launchd schedules for the same job.

The Board Room refresh is part of `com.hwl.nightly-backup`, not a separate schedule. Live edits are stored as append-only events in a private Vercel Blob store. The backup reconciles those events into `this-week.md`, generates `board-room/app/generated-board.json`, runs a frozen dependency install, low-threshold audit, lint, 16 tests and a production build. It stages the backup only after validation, blocks secrets with gitleaks, requires a successful commit and push, then verifies the pushed SHA against `origin/main`. Deployment repeats validation in a clean detached checkout and uses the exact locked Vercel CLI. Deploy-side validation runs the board generator in verify-only frozen-snapshot mode (`HWL_BOARD_FROZEN_SNAPSHOT=1`): it recomputes the board from the committed sources, fails if the committed snapshot has drifted, and writes nothing, so the checkout stays byte-identical to the pushed commit. The deploy is still refused if validation mutates the checkout. Passwordless production page and API reads must return 200 with noindex retained. Git backup and Vercel deployment failures remain separately labelled.

Codex-side production automation is intentionally deferred until the action gateway is connected. The first suitable production Codex automation is a read-only engineering audit that runs tests, runs `scripts/doctor.py`, reviews agent failures, and reports findings without editing business state or sending externally. It must not overlap morning-brief, weekly-review, weekly-cfo, Telegram, health sync, backup, or campaign schedules. A user-authorised Codex thread heartbeat named `Grok Bot Content Watch` runs Thursdays at 09:20. It is a narrow read-only monitor for Grok Bot product changes and attributable workflows, checked against `content/distribution-product-plan-2026-08-10.md`; it cannot authenticate, post, message, purchase, edit business state or create schedules, and is not a production operator.

## Installed foundation

`main` includes the Jarvis safety and durability foundation and the 3 August security and reliability sweep.

Verified on the Mac mini:

- 58 automated tests pass on system Python. The original Jarvis suite and the new automation, health-ingest and Telegram security contracts remain green.
- The Board Room has 16 passing behaviour and security tests. Its production build, lint and low-threshold dependency audit pass with no known vulnerabilities.
- `scripts/doctor.py` reports 16 passed, 1 warning and 0 failures on 10 August. The warning is the deliberately preserved live working paths.
- The morning brief withholds unchecked content if its verifier fails.
- Telegram intake has a durable SQLite queue, private chat and sender checks, redacted task logging, restart recovery, `needs_review` quarantine for interrupted work, and explicit separation of forwarded third-party material from Harrison's own instructions.
- The active health environment is pinned in `agents/health-requirements.txt`. The pypdf pin moved to 6.15.0 after CVE-2026-71870 and CVE-2026-71852 turned the health dependency audit red. The Mac mini environment was upgraded to 6.15.0 on 10 August and `pip check` passes. Pillow 12.3.0 remains clean per the same audit. Hosted ingestion requires a complete token, exact HTTPS URL and exact hostname allowlist.
- `jarvis_core/` provides event, run, task, action, approval, and work queue primitives.
- CI validates Python 3.9 and 3.14, shell, JavaScript, JSON and property-list files. Separate path-filtered jobs enforce the Board Room audit, lint, tests and build, the Better at Work summer tracker audit and configuration checks, and the pinned health dependency audit. Every third-party GitHub Action is pinned to an immutable commit. Dependabot monitors all three dependency surfaces and GitHub Actions weekly.

## Live rollout state

- Grok Bot 0.16.0 was audited and narrowed on 12 August. The existing `Scout` bot is now titled `External research runner` and is limited to one bounded public-source assignment at a time. Its duplicate weekday `Daily business brief` routine is disabled, so it does not overlap `com.hwl.morning-brief` or any other launchd workflow. Grok Bot auto-review is on and local-computer execution remains `Ask every time`. Slack is connected but its five readable channels are stale, with the newest visible activity dated 22 April 2026, so Slack monitoring is not a supported Grok Bot use case. The Vercel plugin is installed but unauthenticated, GitHub is not connected, and neither should be connected without a specific job. Keep Grok Bot on public-web research until Cursor Privacy Mode can be verified for the account. The trial allocation reached 100% during the public-source audit, with seven days left in the trial window. No upgrade was purchased and the private edit-studio pilot did not run. No production scheduler or Claude workflow changed.
- The foundation and coordination handoff are on GitHub `main`. CI passed for the foundation push and the following nightly backup.
- Nightly backup was manually reconciled on 27 July after five misleading failure reports. `agents/nightly-backup.sh` records the exact push, fetch, rebase or retry error and exits non-zero on a real failure instead of labelling every failure as a merge conflict. On 10 August the live worktree was safely rebased onto the merged system repair after its runtime changes were committed. No live state was discarded.
- Harrison explicitly removed the Board Room password on 11 August. The production page, assets and API reads are intentionally public to anyone with the URL, and a direct visitor can use the editing controls. Mutations still require an exact same-origin request, but that is cross-site request protection, not user authentication. Production retains a nonce CSP, private no-store caching, noindex, frame denial and same-origin resource controls. Do not put credentials, private source documents or other secrets on the board. The obsolete Vercel username and password variables and macOS Keychain password were removed after the passwordless deployment passed its live checks.
- The Board Room's `brace-expansion`, PostCSS and locked Vercel CLI transitive advisories are patched. `pnpm audit --audit-level low` reports no known vulnerabilities.
- The nightly Board Room deploy was blocked on 4 and 5 August because deploy-side validation restamped the tracked snapshot. Frozen-snapshot verification fixed that on 6 August. The 6 to 9 August deploys were then blocked by js-yaml GHSA-5p4m-2wfm-xmqj and nanoid GHSA-2v37-7h3g-55p8. PR #12 merged both dependency patches at 06:44 on 10 August. Its post-merge Board Room check exposed two unrelated, state-dependent test defects when the new weekly board arrived: a hard-coded requirement for two completions at the start of a week, and inconsistent ordering for equal scores. PR #20 merged the repair at 07:53 on 10 August with every branch and pull-request check green. Audit, lint, all 16 tests and the production build pass. Production was refreshed on 10 August with the current weekly snapshot after a clean-checkout validation and successful authenticated live-state check.
- The Better at Work summer tracker was hardened and deployed at 20:43 BST on 3 August. Writes now require the edit key, exact same-origin requests, JSON content and bounded payloads. Browser security headers are live and its dependency audit reports no known vulnerabilities.
- The separate Better at Work public-site branch was finished for Cathal's end-of-week review on 6 August. It adopts the approved original outlined mark across masthead, footer, icon and social preview, refreshes the Acast snapshot to 85 episodes including Better Moments #4, and wires Better Careers checkout end to end: a Stripe Checkout API route, buy buttons and a noindex confirmation page. Checkout is dormant with truthful cannot-take-payment states until `STRIPE_SECRET_KEY` and `STRIPE_CHECKOUT_PRICE_ID` are set; price, currency, refund terms and fulfilment remain undecided client decisions. Content verification, typecheck, the 99-page build and low-threshold audit pass locally and in GitHub Actions. A same-day polish pass verified the site against the Brand Playground Figma variables, repaired feed whitespace and markdown artefacts at render time, reweighted the Work Problem Finder toward the visitor's own words, added a branded 404, and corrected the dead Spotify and YouTube footer links. The protected Vercel preview was redeployed from the merged commit and still fails closed behind Vercel SSO. The branch remains unmerged to `main` and production at betteratwork.net is unchanged. A pre-merge safety stash of the previously uncommitted brand alignment sits in the `HWL META-baw-site` worktree, superseded by commit `81e680c`.
- Vercel could not attach the repository because the Vercel account has no GitHub login connection. The existing nightly backup therefore owns the refresh and production deployment path through the locally linked Vercel project. No additional scheduler was created.
- Morning brief, verifier, weekly review and campaign chaser source rules were corrected on 27 July. They must verify the connected Gmail identity, check live Creepers and Better at Work dashboards, validate draft claims against Gmail, use only the canonical training plan, and diagnose a failure before escalating it.
- Publication-source precedence was corrected on 31 July after the Creepers calendar falsely contradicted content that was already live. Planning dashboards now govern planned work and queue order only. Harrison's explicit correction, the live social post or Buffer's published record governs whether content shipped. The existing Buffer MCP needs re-authentication before it can provide unattended published and failed-post checks. No new scheduler was created.
- Buffer was audited read-only on 3 August. The existing project-local endpoint is correctly registered at `https://mcp.buffer.com/mcp`, but Claude reports `Needs authentication` and Codex reports `Not logged in`. The morning brief and verifier already contain published, scheduled and failed-post checks from commit `ff82ad4`. On 4 August Harrison cancelled the Buffer OAuth task. No re-authentication or channel inventory is planned. Unattended Buffer-backed published and failed-post checks stay disabled, and briefs must not claim them. Keep `com.hwl.morning-brief`; do not add a Buffer-specific scheduler.
- The main Claude CLI OAuth session expired before the 2 August learning brief. Learning brief, weekly review, morning brief, content engine, campaign chaser and discovery scan then failed through 3 August. Current `today.md` and `this-week.md` were rebuilt manually and are not evidence of successful scheduled runs.
- Every Claude-backed wrapper now runs a non-interactive auth preflight before touching handoff files or attempting agent work. Authentication failures exit 78, write per-job stdout and stderr, record an atomic latest-run JSON under `.jarvis-runtime/agent-runs/`, and preserve the real CLI exit code. A live weekly-review probe at 20:56 correctly exited 78 with `auth_required` and performed no workflow work. Scheduled Claude runs now use Claude's current `auto` safety mode instead of blanket permission bypass.
- Claude authentication was restored on 6 August. `claude auth status --text`, the 10 August doctor run and every scheduled Claude-backed job since the repair confirm the session is live. Buffer remains a separate, deliberately unaddressed OAuth gap.
- The stale-state incident on 27 July and the OAuth outage beginning 2 August are failed acceptance runs. Acceptance remains reset until all three v1 agents complete two clean unattended weeks.
- `com.hwl.telegram-agent` was moved from blanket `bypassPermissions` to Claude's current `auto` safety mode and restarted at 21:15 BST on 3 August. Its existing restricted deny profile remains in force. The private durable queue was empty and no task was interrupted. The capture-only Telegram path now requires both an allowlisted sender and matching private chat, and all Telegram state files are mode 0600.
- Full gitleaks scans of all 129 commits on `main` and the Better at Work branch found no secrets in Git history. Staged backups are now blocked if gitleaks detects a secret.
- The dormant trading-bot archive was re-audited on 13 August. `hwlstudio-ai-agency/projects/trading-bot` now holds only a `logs` directory: the vulnerable virtual environment with its 108 advisories is gone, and no `.env` or other credential-bearing file remains anywhere under `hwlstudio-ai-agency`. Its three launchd plists sit in `LaunchAgents-disabled-2026-06-25`, none are loaded, and no matching process runs. The local exposure is therefore closed. **The old Kraken API credential and the old Telegram bot token remain valid at their providers until Harrison revokes them, and that is still the outstanding action.** Deleting the local copies did not revoke anything.
- The next non-command task is the first live proof that the new process persists before acknowledgement, writes only a task fingerprint to logs and completes successfully under auto mode.
- The 14 July morning brief used the fail-closed verifier path and sent Telegram message 536 after checking 30 claims and correcting one. The missing Monday 13 July run should be monitored for recurrence.
- Pre-restart Telegram logs still contain legacy task previews. They are ignored by Git; future task logs should use fingerprints.
- Linear sync entered an hourly flap on HWL-191 from 9 to 10 August because `today.md` and `this-week.md` carried the same marker with opposite checkbox states. PR #20 gives each issue one state-recorded owning Markdown file, makes duplicate or stale markers fail closed, prevents unmarked Today tasks creating duplicate issues, and adds CI coverage. Five stale collision issues were cancelled. A live 06:55 sync created fresh HWL-247 to HWL-255 issues, closed HWL-231 and HWL-244, and made zero HWL-191 state writes. Agent prompts now forbid inventing or recycling Linear markers.
- `agents/telegram-agent.py` was repinned from `claude-opus-4-8` to `claude-opus-5` on 13 August by a parallel Claude session, and the daemon was restarted with `launchctl kickstart`. Independently verified from this session: `MODEL = "claude-opus-5"` at line 62, `com.hwl.telegram-agent` running as PID 1138 started 10:55:55 against a file modified at 10:55, so the live daemon is executing the new code. The prior `-15` exit status is the previous instance's SIGTERM from the kickstart, not a fault. The full 61-test suite passes after the change. No schedule was created or altered.
- **History rewrite executed 13 August. `main` moved from `d1f07c5` to `117fb3f`.** Harrison authorised removing the leaked financial and identity documents from Git history. `git filter-repo` stripped `tmp/pdfs/asb-statement*`, `tmp/pdfs/milford*` and the draft KiwiSaver PDF from all 167 commits, then the rewritten history was force-pushed to `origin/main`. Every commit has a new SHA, so any clone, worktree or checkout predating the rewrite is divergent and must re-sync with `git fetch origin && git reset --hard origin/main` rather than push. Ten `refs/codex/turn-diffs/checkpoints/*` refs held the same blobs as raw trees and were deleted, because `filter-repo` skips non-commit refs and leaving them would have kept the documents reachable; Codex turn-diff checkpoints from 11 and 12 August no longer exist. Reflog was expired and an aggressive gc run. Purge is verified, not assumed: blob `a23ab9f1` (ASB statement) is confirmed unretrievable via `git cat-file`, zero refs carry the paths, and `.git` fell from 7.0 GB to 5.2 GB. `git filter-repo` removed the `origin` remote and every remote-tracking ref as a side effect, which would have blocked tonight's deploy at `nightly-backup.sh` line 285; both the remote and `main`'s upstream tracking were restored afterwards and the backup's own line 204 and line 285 checks were simulated and pass. LFS survived intact, `git lfs fsck` reports OK across all 13 objects. `codex/baw-frontier-site` never descended from the offending commit and is unchanged at `cce3f68`; the BaW safety stash was rewritten in place by filter-repo and additionally preserved as a patch. Rollback artifacts are in the session scratchpad: a 2.2 GB bundle of all 39 pre-rewrite refs, a ref SHA map, the stash patch and copies of the uncommitted working files. **GitHub may retain the old unreachable objects in its own storage until its garbage collection runs; the repository is private and single-owner, so residual risk is low, but a support request is the only way to force that.** Doctor reports 16 passed and 0 failed, the suite is 61 tests all passing, and gitleaks finds no leaks.
- Restructure Phases 2, 3 and 4 landed 13 August. **Phase 2:** `agents/STATE-OWNERSHIP.md` declares an owner for every state file, and `evening-reflection` no longer writes `today.md`, removing the third writer from the hottest file. `scripts/check-state-consistency.mjs` fails if a `linear:HWL-` marker appears in more than one state file or twice within one, which is the HWL-191 flap encoded as a test; it runs in `nightly-backup.sh` warn-only, because bad state is not a secret and must not block backing up everything else. **Phase 3:** `agent-runtime.sh` now alerts by Telegram when the shared Claude session is logged out. The preflight always exited 78 correctly but nothing listened, which is how the 2 to 6 August outage ran four days. De-duplicated to at most one message per six hours across all 17 jobs, cleared the moment auth recovers, failing open on a corrupt sentinel or missing config. **Phase 4:** migration 2 adds resource-scoped leases to `jarvis_core`; an item naming a resource is claimable only when no other running unexpired lease holds it, so a claim can mean "I am writing this file", while items naming no resource still claim concurrently. Live writers are deliberately **not** migrated onto it yet: that needs a working Claude session to verify against. Suite is 90 tests, doctor 15 passed 0 failed.
- Auth outage forensics, 13 August, occurrence three. Evidence captured while live rather than after another silent recovery. The credential lives in the macOS Keychain as `Claude Code-credentials`, not in a `~/.claude/.credentials.json` file. Keychain timestamps: created `20260518174515Z`, modified `20260813143331Z`, so the item had stood for roughly three months before failing, which does not look like a fixed time-to-live. Last known good is `06:35:12` (morning-brief logged `AUTH_OK` three times between 06:30 and 06:35). First known bad is around 12:50 when this session ran doctor. **No Claude-backed scheduled job ran in that six-hour window**, so nothing observed the transition; the only entries in between are Codex board updates at 12:05 and 12:34, which do not use CLI auth. Root cause remains undiagnosed, but the window is now bounded and the artefacts are recorded. The Telegram agent rides the same CLI session and was therefore down for the same window. Harrison re-authenticated at 15:33 BST and `claude auth status --text` now reports a logged-in Claude Max account.
- **Claude CLI was logged out on 13 August afternoon.** `claude auth status --text` reports "Not logged in". Every scheduled agent will exit 78 until Harrison runs `claude auth login` interactively. Doctor caught it only because a session happened to run doctor, which is precisely the gap Phase 3 now closes.
- Disk investigation, 13 August. Free space fell from roughly 93 GB at 13:00 to about 30 GB by 15:20, with no single large consumer. Two causes, neither a leak. First, iCloud is reconciling: `bird` was at 98.4% CPU after Optimise Mac Storage was enabled at midday, and macOS materialises files while reconciling before eviction begins, so the space should return. Second, most of the apparent delta is reclassification, not consumption: `Library` fell from 494 GB to 88 GB while `Documents` rose from 30 GB to 454 GB as the iCloud container reorganised, and the two roughly offset. **Operational rule: do not run `du` over `~/Documents`, `~/Downloads` or `~/Library/Mobile Documents`.** Walking a dataless file on macOS can make `bird` materialise it, so scanning iCloud-managed directories feeds the very number being measured. Watch `df` instead, which does not touch files.
- Branch and ref clean-out, 13 August, after Phase 1. Remote branches went from 16 to 3: `main`, `codex/baw-frontier-site` (live Better at Work site, 15 unmerged commits, verified still sharing ancestry with the rewritten main at `ada748f6` and still merging cleanly, so no rebase was needed) and `codex/dream-body-plan` (open draft PR #1). Four branches were deleted after verifying each was merged or redundant: `codex/creepers-session-2` (PR #21), `codex/system-reconciliation-2026-08-10` (PR #20), `claude/sunday-evening-checkin-hdykdc` (PR #12) and `claude/baw-site-finish` (no PR; its only unique content was a `launch.json`, now merged into `.claude/launch.json` beside the `hwl-logo-motion` config). The nine Dependabot pull requests were **closed rather than merged**, each with a comment explaining why: their bases predate the history rewrite, so merging would have reintroduced the removed media into `main`. Nothing was rejected on its merits and Dependabot will re-propose against the rewritten history. Locally, nine stale branches, nine Dependabot branches, five Codex turn-diff refs and 21 `refs/remotes/pr/*` refs were removed; the three 3 August hardening branches were checked feature-by-feature against `main` first (auth preflight with exit 78, SHA-pinned actions and `dependabot.yml`, Board Room same-origin and nonce CSP) rather than trusted by commit ancestry, which is unreliable after a rewrite. Zero refs now carry client media or the financial documents. `.git` holds at 1.4 GB. **Known remaining fat: `tmp/` still occupies 264 MB of history even though it is now gitignored, and `content/inspo/raw` holds 237 MB of downloaded reference video.** Both are candidates for the next rewrite. That rewrite is deliberately deferred: two chip-spawned worktree sessions currently occupy `claude/lucid-goldstine-eb493c` and `claude/sharp-bartik-396cb4`, and rewriting would disturb live sessions for a roughly 19% gain. Batch it with the next planned rewrite once those sessions close.
- Sensitive-document exposure found and closed, 13 August. `tmp/` was tracked, not ignored, and the 12 August commit `31a9fbd` pushed two ASB bank statement images and sixteen completed Milford KiwiSaver form images plus the draft KiwiSaver PDF to `origin/main`. The repository is private and single-owner, and a full gitleaks scan of all 198 commits still reports no leaks, because gitleaks matches credential patterns in text and cannot see inside a scanned document. That is the blind spot. Three fixes are now in place: the 21 documents moved to `private/`, `private/` and `tmp/` are both gitignored and untracked, and `agents/nightly-backup.sh` gained a path-based guard that runs before both the secret scan and the commit and aborts the backup if a financial or identity document is staged. The guard is scoped to document and image extensions so prose that merely mentions a statement still commits normally. Three tests cover it, including one that executes the shell matcher against real leaked paths and against files that must stay allowed. The suite is 61 tests, all passing. **Still open and Harrison's call: the documents remain in Git history on `origin/main`.** Removing them needs a `git filter-repo` history rewrite and a force push, which would break the nightly backup's pushed-SHA verification and must be coordinated with Codex. Given a private single-owner repository, the residual risk is low but not zero.
- Disk cleanup, 13 August. The Mac mini was at 98% full with 19 GB free, which put the nightly backup, the Board Room build and all video work at risk. Four merged worktrees were removed (`HWL META-baw-jennifer-moss`, `HWL META-board-room`, `HWL META-system-reconciliation`, `.claude/worktrees/upbeat-knuth-d31e89`) plus the merged `fuckaroundfindout-jarvis` worktree, along with six local branches carrying zero commits not in `main`. `HWL META-baw-site` is deliberately retained: it holds 15 unmerged commits and the live Better at Work site. Its `stash@{0}` safety stash lives in the shared repository and is unaffected. Every removed worktree was verified clean and fully merged first, and its Git LFS media confirmed present in the main checkout. Worktrees of this repository cost 2 to 4 GB each because each one materialises its own physical copy of the LFS podcast masters, so future engineering worktrees should be removed with `git worktree remove` as soon as their branch merges. Free space went from 19 GB to 100 GB. `tmp/` was inspected and deliberately not cleared: it is not gitignored, holds 375 tracked files, and contains the live Milford KiwiSaver forms and ASB bank statement images.
- Four production faults were repaired on 14 August morning, all verified live. **Board Room:** nanoid advisory GHSA-2v37-7h3g-55p8 widened to <3.3.18 during 13 August with no dependency change on our side, re-failing the audit gate and blocking the 22:30 deploy; the override moved from 3.3.17 to 3.3.18 and audit, lint, all 16 tests and the production build pass, so tonight's deploy is unblocked. **Root CI:** the python jobs' sparse checkout never fetched `today.md`, `this-week.md`, `agents/STATE-OWNERSHIP.md` or `agents/evening-reflection.md`, so the Phase 2 contract tests failed on FileNotFoundError three runs in a row; the four files are now in the checkout list, and the committed snapshot was verified consistent before pushing. **Linear:** the workspace crossed Linear's free-plan cap of 250 unarchived issues (275 at diagnosis), so every hourly `issueCreate` failed with "usage limit exceeded" from 13 August ~07:00 to 14 August 08:00, a roughly 25-hour sync outage; `linear/archive-closed.js` archived 219 issues closed more than 7 days with no live `this-week.md` marker (reversible per-issue), and a kickstarted sync then created HWL-276 to HWL-279 cleanly. **Recurrence risk is real: at the current issue creation rate the cap returns in roughly three weeks.** No archive schedule was created, deliberately; the options are a periodic `archive-closed.js` step in the nightly backup or a Linear upgrade, and that is Harrison's call. **Lab sends:** form-lab's 13 August message parked unsent because agent Claude runs under acceptEdits and curl needs interactive approval; `agent-runtime.sh` gained a generic `hwl_send_telegram`, form-lab and pattern-lab now write their one message to `agents/outbox/<agent>-<date>.md` and the wrapper delivers it after the run, the same wrapper-side send architecture the morning brief already uses. `STATE-OWNERSHIP.md` records the outbox convention, form-lab's prose no longer claims a write to `content/pipeline.md` (content-engine owns it, and the 13 August run had correctly refused the write), and the parked 13 August message was delivered through the new helper as its live test. Suite is 90 tests all passing, doctor 16 passed 0 failed.
- No Codex automation has been created.

## Safety boundary

The foundation is not yet a hard permission boundary around Claude.

- There is no generic action executor.
- Scheduled Claude wrappers and the user-driven Telegram agent now use Claude's current `auto` safety mode. No live agent path retains `bypassPermissions`. The project-local permanent permission allowlist is **no longer empty**: `.claude/settings.json` gained 12 `permissions.allow` entries on 13 August via the `/fewer-permission-prompts` skill. Independently verified as read-only: Gmail `search_threads`, `get_thread` and `list_drafts`, Calendar `list_events` and `search_events`, Strava `list_activities`, Granola `list_meetings`, exa `web_search_exa`, and `Bash(sqlite3 -readonly *)`. Nothing in the list can send, create, modify or delete. The `deny` list is empty, so the Telegram agent's restricted deny profile remains the separate control it always was. Authorisation is verified from Harrison's own words, not from a peer's report: his 13 August instruction in the `Multi-agent workflow with Chord` session reads "Let's go run the fewer permission prompts to fill the interactive allow list with safe read-only commands." Read directly from that transcript, so this is primary evidence.
- The old Kraken and Telegram credentials in the ignored archive are contained locally but remain valid until Harrison revokes them at their providers.
- Email, messaging, calendar, file, and other connectors are not yet routed through typed Jarvis adapters.
- Approval actor names in the current CLI are unauthenticated assertions and must not authorise a real external side effect.
- Do not describe a legacy action as approval-gated merely because a proposal can be recorded in the ledger.

## Next implementation sequence

1. Revoke the old Kraken API credential and old Telegram bot token found in the ignored trading-bot archive. Keep the archive dormant until it is rebuilt from a fresh, audited environment.
2. Watch the restored morning-brief and weekly-review acceptance streaks. Authentication is live, but output accuracy and session longevity remain separate checks.
3. Watch the next hourly Linear run for a second zero-flap receipt. The repair is deployed and the stale collision issues are closed.
4. Skipped. Harrison cancelled the Buffer OAuth task on 4 August. No Buffer re-authentication or channel inventory is planned.
5. Confirm the next non-command Telegram task appears in the durable queue lifecycle and logs only its fingerprint.
6. Add dry-run typed adapters for one reversible internal action first.
7. Add authenticated approval ingress, execution leases, destination receipts and independent verification.
8. Route external actions through the gateway one adapter at a time. Remove broad legacy permissions only when the replacement path is proven.
9. After the gateway is enforcing policy, consider one read-only Codex engineering audit automation.

## Coordination rules

Before any system change:

1. Run `git status --short --branch`.
2. Do not discard, stage, or commit unrelated working changes.
3. Use a separate worktree or branch for substantial changes.
4. Run the relevant tests and `scripts/doctor.py`.
5. Merge one scoped commit.
6. Remove the worktree with `git worktree remove` once its branch is merged, and delete the merged branch.
7. Update this file if rollout state or safety boundaries changed.

Full architecture and rollback guidance: `JARVIS-FOUNDATION.md`.
