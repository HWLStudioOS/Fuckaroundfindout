[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fuckaroundfindout-jarvis"
version = "0.1.0"
description = "A local-first personal operations and assistant foundation."
readme = "README.md"
requires-python = ">=3.9"
dependencies = []

[tool.setuptools.packages.find]
include = ["jarvis_core*"]
exclude = ["tests*"]
