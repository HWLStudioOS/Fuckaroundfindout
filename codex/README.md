# Harrison OS

This workspace contains Harrison's second-brain / operating-system memory for HWL Studio, personal systems, learning, finance, health, and client work.

For Claude Code, start in this folder:

```bash
cd /Users/harrison/hwlstudio-codex
claude
```

Claude Code should read `CLAUDE.md` first, then use the Markdown files in `harrison-os/` as the durable memory.

For the next concrete task, see `NEXT_ACTIONS.md`.

For the full handoff from today's Codex interview/build session, see `HANDOFF_2026-05-11.md`.
